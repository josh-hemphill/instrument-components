// <auto-generated by tools/gen-opentap-steps.ts />
using OpenTap;

namespace InstrumentComponents.OpenTap;

[Display("Read Totalize", Groups: ["Instrument Components", "Counter", "Totalize"], Description: "Read the totalize count.")]
public sealed class CounterReadTotalizeStep : OptionalLimitStep<CounterInstrument>
{
    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        PublishScalar("Totalize", instrument.Counter.ReadTotalize(), "");
    }
}

[Display("Reset Totalize", Groups: ["Instrument Components", "Counter", "Totalize"], Description: "Reset the totalize count.")]
public sealed class CounterResetTotalizeStep : InstrumentBoundStep<CounterInstrument>
{
    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Counter.ResetTotalize();
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Select Channel", Groups: ["Instrument Components", "Counter", "Configure"], Description: "Select the counter input channel.")]
public sealed class CounterSelectChannelStep : InstrumentBoundStep<CounterInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    public CounterSelectChannelStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Counter.SelectChannel(Channel);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Gate Time", Groups: ["Instrument Components", "Counter", "Configure"], Description: "Set the counter gate / aperture time.")]
public sealed class CounterSetGateTimeStep : InstrumentBoundStep<CounterInstrument>
{
    [Display("Gate time", Order: 2)]
    [Unit("s")]
    public double Seconds { get; set; } = 0.1;

    public CounterSetGateTimeStep()
    {
        Rules.Add(() => double.IsFinite(Seconds), "Gate time must be a finite number.", nameof(Seconds));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Counter.SetGateTime(Seconds);
        UpgradeVerdict(Verdict.Pass);
    }
}
