// <auto-generated by tools/gen-opentap-steps.ts />
using OpenTap;

namespace InstrumentComponents.OpenTap;

[Display("Run", Groups: ["Instrument Components", "Scope", "Acquire"], Description: "Start continuous acquisition.")]
public sealed class ScopeRunStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.Run();
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Channel Coupling", Groups: ["Instrument Components", "Scope", "Configure"], Description: "Set AC/DC/GND coupling for one channel.")]
public sealed class ScopeSetChannelCouplingStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Coupling", Order: 3)]
    public string Coupling { get; set; } = "DC";

    public ScopeSetChannelCouplingStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetChannelCoupling(Channel, Coupling);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Channel Display", Groups: ["Instrument Components", "Scope", "Configure"], Description: "Enable or disable a scope channel.")]
public sealed class ScopeSetChannelDisplayStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Display enabled", Order: 3)]
    public bool DisplayEnabled { get; set; } = true;

    public ScopeSetChannelDisplayStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetChannelDisplay(Channel, DisplayEnabled);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Channel Scale", Groups: ["Instrument Components", "Scope", "Configure"], Description: "Set volts per division for one channel.")]
public sealed class ScopeSetChannelScaleStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Volts / div", Order: 3)]
    [Unit("V")]
    public double VoltsPerDiv { get; set; } = 1;

    public ScopeSetChannelScaleStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
        Rules.Add(() => double.IsFinite(VoltsPerDiv), "Volts / div must be a finite number.", nameof(VoltsPerDiv));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetChannelScale(Channel, VoltsPerDiv);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Timebase Scale", Groups: ["Instrument Components", "Scope", "Configure"], Description: "Set seconds per division.")]
public sealed class ScopeSetTimebaseScaleStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Seconds / div", Order: 2)]
    [Unit("s")]
    public double SecondsPerDiv { get; set; } = 0.001;

    public ScopeSetTimebaseScaleStep()
    {
        Rules.Add(() => double.IsFinite(SecondsPerDiv), "Seconds / div must be a finite number.", nameof(SecondsPerDiv));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetTimebaseScale(SecondsPerDiv);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Trigger Level", Groups: ["Instrument Components", "Scope", "Trigger"], Description: "Set the edge trigger level.")]
public sealed class ScopeSetTriggerLevelStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Level", Order: 2)]
    [Unit("V")]
    public double Volts { get; set; }

    public ScopeSetTriggerLevelStep()
    {
        Rules.Add(() => double.IsFinite(Volts), "Level must be a finite number.", nameof(Volts));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetTriggerLevel(Volts);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Trigger Slope", Groups: ["Instrument Components", "Scope", "Trigger"], Description: "Set the edge trigger slope.")]
public sealed class ScopeSetTriggerSlopeStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Slope", Order: 2)]
    public string Slope { get; set; } = "POS";

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetTriggerSlope(Slope);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Trigger Source", Groups: ["Instrument Components", "Scope", "Trigger"], Description: "Set the edge trigger source.")]
public sealed class ScopeSetTriggerSourceStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    [Display("Source", Order: 2)]
    public string Source { get; set; } = "CHAN1";

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.SetTriggerSource(Source);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Single", Groups: ["Instrument Components", "Scope", "Acquire"], Description: "Acquire a single trace.")]
public sealed class ScopeSingleStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.Single();
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Stop", Groups: ["Instrument Components", "Scope", "Acquire"], Description: "Stop acquisition.")]
public sealed class ScopeStopStep : InstrumentBoundStep<OscilloscopeInstrument>
{
    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Scope.Stop();
        UpgradeVerdict(Verdict.Pass);
    }
}
