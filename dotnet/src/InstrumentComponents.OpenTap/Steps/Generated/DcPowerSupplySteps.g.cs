// <auto-generated by tools/gen-opentap-steps.ts />
using OpenTap;

namespace InstrumentComponents.OpenTap;

[Display("Output Enable", Groups: ["Instrument Components", "PSU", "Configure"], Description: "Enable or disable one PSU output.")]
public sealed class PsuOutputEnableStep : InstrumentBoundStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Output enabled", Order: 3)]
    public bool OutputEnabled { get; set; } = true;

    public PsuOutputEnableStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Supply.OutputEnable(Channel, OutputEnabled);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Query Output State", Groups: ["Instrument Components", "PSU", "Measure"], Description: "Query whether one PSU output is enabled.")]
public sealed class PsuOutputStateQueryStep : OptionalLimitStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    public PsuOutputStateQueryStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        PublishScalar("Output State", instrument.Supply.OutputStateQuery(Channel) ? 1.0 : 0.0, "");
    }
}

[Display("Enable OVP", Groups: ["Instrument Components", "PSU", "Protect"], Description: "Enable or disable over-voltage protection.")]
public sealed class PsuOvpEnableStep : InstrumentBoundStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("OVP enabled", Order: 3)]
    public bool OvpEnabled { get; set; } = true;

    public PsuOvpEnableStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Supply.OvpEnable(Channel, OvpEnabled);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set OVP Level", Groups: ["Instrument Components", "PSU", "Protect"], Description: "Set over-voltage protection level.")]
public sealed class PsuOvpLevelStep : InstrumentBoundStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Voltage", Order: 3)]
    [Unit("V")]
    public double Voltage { get; set; }

    public PsuOvpLevelStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
        Rules.Add(() => double.IsFinite(Voltage), "Voltage must be a finite number.", nameof(Voltage));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Supply.OvpLevel(Channel, Voltage);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Query OVP", Groups: ["Instrument Components", "PSU", "Protect"], Description: "Query whether over-voltage protection is enabled.")]
public sealed class PsuOvpQueryStep : OptionalLimitStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    public PsuOvpQueryStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        PublishScalar("OVP", instrument.Supply.OvpQuery(Channel) ? 1.0 : 0.0, "");
    }
}

[Display("Read Current", Groups: ["Instrument Components", "PSU", "Measure"], Description: "Read back one PSU channel current.")]
public sealed class PsuReadCurrentStep : OptionalLimitStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    public PsuReadCurrentStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        PublishScalar("Current", instrument.Supply.ReadCurrent(Channel), "A");
    }
}

[Display("Read Voltage", Groups: ["Instrument Components", "PSU", "Measure"], Description: "Read back one PSU channel voltage.")]
public sealed class PsuReadVoltageStep : OptionalLimitStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    public PsuReadVoltageStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        PublishScalar("Voltage", instrument.Supply.ReadVoltage(Channel), "V");
    }
}

[Display("Set Current Limit", Groups: ["Instrument Components", "PSU", "Configure"], Description: "Set the current limit of one PSU channel.")]
public sealed class PsuSetCurrentLimitStep : InstrumentBoundStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Current limit", Order: 3)]
    [Unit("A")]
    public double CurrentLimit { get; set; }

    public PsuSetCurrentLimitStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
        Rules.Add(() => double.IsFinite(CurrentLimit), "Current limit must be a finite number.", nameof(CurrentLimit));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Supply.SetCurrentLimit(Channel, CurrentLimit);
        UpgradeVerdict(Verdict.Pass);
    }
}

[Display("Set Voltage", Groups: ["Instrument Components", "PSU", "Configure"], Description: "Set the output voltage of one PSU channel.")]
public sealed class PsuSetVoltageStep : InstrumentBoundStep<DcPowerSupplyInstrument>
{
    [Display("Channel", Order: 2)]
    public uint Channel { get; set; } = 1;

    [Display("Voltage", Order: 3)]
    [Unit("V")]
    public double Voltage { get; set; }

    public PsuSetVoltageStep()
    {
        Rules.Add(() => Channel >= 1, "Channel must be at least 1.", nameof(Channel));
        Rules.Add(() => double.IsFinite(Voltage), "Voltage must be a finite number.", nameof(Voltage));
    }

    public override void Run()
    {
        if (!TryGetInstrument(out var instrument))
            return;

        instrument.Supply.SetVoltage(Channel, Voltage);
        UpgradeVerdict(Verdict.Pass);
    }
}
